[project]
name = "llm-bus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pawel Rainer", email = "pawel.rainer@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
llm-bus = "llm_bus:main"

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]
